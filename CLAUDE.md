# CLAUDE.md — KS

@AGENTS.md

Use `AGENTS.md` as the instruction source of truth, then read `README.md` for
the verified business facts and the open items. Shared policy lives under
`docs/standards/`; the project profile and the pinned governance baseline live
in `.web-design/project.json` and `.web-design/lock.json`.

The production deployment stack is project-owned and currently parked for the
migration. Read `docs/migration/README.md` before touching anything under
`website/production/`, `scripts/wait-for-production-checks.mjs`, or
`docs/migration/pending/`.
